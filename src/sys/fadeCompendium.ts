export class fadeCompendium extends Compendium {
   /** @inheritDoc */
   //async _render(...args) {
   //   await super._render(...args);
   //   let items = this.collection;
   //   if (this.collection.index) {
   //      if (!this.collection._reindexing) this.collection._reindexing = this.collection.getIndex();
   //      await this.collection._reindexing;
   //      items = this.collection.index;
   //   }
   //   for (const item of items) {
   //      if (items.has(item.system?.cost > 0)) {
   //         const costElement = $(`<div class="item-cost">Cost: ${item.system?.cost}</div>`);
   //         this._element?.[0].append(costElement);
   //      }
   //   }
   //}
}
